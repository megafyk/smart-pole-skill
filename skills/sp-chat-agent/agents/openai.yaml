interface:
  display_name: "SMART POLE Chat Enforcer"
  short_description: "Enforce strict prompt readiness with XML handoff"
  default_prompt: "Use $sp-chat-agent to enforce clarification and output only a final <master_prompt> XML block."
